import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0B1020);
  static const Color surface = Color(0xFF131A2E);

  // Accents
  static const Color primary = Color(0xFF5865F2);   // Discord/Linear Blue
  static const Color secondary = Color(0xFF22D3EE); // Cyan AI vibe

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Borders & Glass
  static const Color glassBorder = Color(0x1AFFFFFF); // White with 10% opacity
  static const Color glassHighlight = Color(0x0FFFFFFF); // White with 6% opacity
}